// DESCRIPTIONS des fiches de fusion de la Ligue (FUSIODEX) — clé = id de fiche (leagueFusionId(name)).
//   Rempli par lot (génération). Repli auto « Fusion de la Ligue née de X et Y. » si absent.

export const LEAGUE_FUSION_DESC: Record<string, string> = {
    // ── Rempli par lot (génération L'Archiviste / fusions de Ligue). ──
}
